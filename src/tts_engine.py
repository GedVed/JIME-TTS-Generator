from TTS.api import TTS
import torch
import os


#device = "cuda" if torch.cuda.is_available() else "cpu"
#tts2 = TTS("voice_conversion_models/multilingual/multi-dataset/openvoice_v2").to(device)
#tts = TTS("tts_models/pl/mai_female/vits").to(device)
#tts = TTS("tts_models/pol/fairseq/vits").to(device)
#tts = TTS("tts_models/multilingual/multi-dataset/xtts_v2").to(device)